# Hello mineproj

这是 **mineproj** 的示例项目内容，用于演示 `index.json` + `body.<locale>.md` 的数据模型。

- 项目名、标语、简介来自 `index.json`
- 本页长文来自 `body.zh-CN.md`，由 Markdown 管线在构建期渲染（Shiki 高亮、目录、摘录）
- 截图与 PDF 等资产按约定放入 `images/` 与 `docs/`
