{
  "name": "@mineproj/e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright smoke tests for mineproj static output",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "node ../../packages/cli/dist/cli.js build --root ../../examples/basic",
    "test": "pnpm run build && playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
