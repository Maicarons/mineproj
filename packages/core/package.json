{
  "name": "@mineproj/core",
  "version": "0.0.0",
  "description": "mineproj core: config parsing, data loading, route generation, theme resolution, plugin hooks, artifact emission",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsdown",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "peerDependencies": {
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "tsdown": "^0.12.0",
    "vitest": "^4.0.0"
  }
}
